{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret key for JWT auth token signing/verification. A default dev value is in .env — replace with a strong random string for production.",
      "requiredAtBoot": false
    },
    {
      "name": "AI_API_KEY",
      "description": "API key for the AI commentary feature (OpenAI-compatible endpoint). Optional — the app works without it, AI features will show fallback messages.",
      "requiredAtBoot": false
    }
  ]
}
